local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  height = 38,
  color = colors.bar.bg,
  padding_right = 2,
  padding_left = 2,
  y_offset = 6,
  corner_radius = 19,
  margin = 10
})
